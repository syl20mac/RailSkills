# Règles de Projet - RailSkills (iOS & Web)

Vous êtes l'assistant intelligent pour le projet RailSkills.
**Langue de réponse obligatoire : FRANÇAIS**

## 1. Contexte & Architecture

RailSkills est un écosystème de suivi des compétences pour les conducteurs de train (CTT).
- **iOS** : Client lourd, SwiftUI, MVVM, Offline-first, Synchro SharePoint.
- **Web** : Client léger, React/TypeScript, Node.js, Dashboard de consultation.

## 2. Règles d'Or

### 🗣️ Communication
- **Toujours répondre en FRANÇAIS.**
- Utiliser l'anglais pour le code (noms de variables, fonctions, commits).
- Documentation et commentaires : **FRANÇAIS**.

### 💻 Stack iOS
- Swift 5+, SwiftUI, Combine.
- Architecture MVVM.
- **Point Critique** : `SharePointSyncService.swift` (ne jamais modifier sans vérifier la compatibilité Web).
- **Sécurité** : `EncryptionService.swift` (AES-256-GCM).

### 🌐 Stack Web
- TypeScript, React, Node.js.
- Backend : Proxy Graph API, Déchiffrement AES.
- Frontend : TailwindCSS, React Query.

### 🛡️ Sécurité
- Chiffrement AES-GCM obligatoire pour les données sensibles.
- Format : `[nonce (12)] + [ciphertext + tag]`.
- Clé dérivée via SHA256(`secret` + `salt`).

## 3. Instructions Spécifiques

1. **Ne jamais casser la synchro** : La structure des fichiers JSON sur SharePoint est sacrée.
2. **Gestion des Erreurs** : Privilégier la robustesse (ne pas crasher l'app, logger l'erreur).
3. **Conventions** : Suivre le style de code existant (indentation, nommage).

---
Référence complète : `GUIDE_PROJET.md` (si disponible).
